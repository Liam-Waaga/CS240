public class PolishNotationEvaluator {
    public double parse(String expression) {
        
        Node<Tokens> token_tree = new Node<Tokens>(null, null, null);

        for (int i = 0; i < expression.length(); i++) {
            /* turn it into a tree */
        }
    }
}